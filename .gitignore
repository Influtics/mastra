node_modules/
# All `.mastra/` files are regenerated by `mastra build` (bundler-config,
# build-manifest, .build/, output/, package-lock.json snapshot, etc.). Never
# commit them — they create noisy diffs and bloat history.
.mastra/
.env
.env.local
.env.*.local
*.log
.DS_Store
